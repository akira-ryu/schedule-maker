<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:subcinctus_factorem"
             x:Class="subcinctus_factorem.MainPage">

    <Grid RowDefinitions="Auto,*">

        <!-- Horizontal list of person buttons -->
        <CollectionView Grid.Row="0"
                ItemsSource="{Binding employees}"
                SelectionMode="Single"
                SelectedItem="{Binding SelectedPerson}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Horizontal"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Button Text="{Binding Title}" 
            Margin="10"
            Command="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=SelectPersonCommand}"
            CommandParameter="{Binding}"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Schedule grid for the selected person -->
        <Border Grid.Row="1"
        Stroke="LightGray"
        StrokeShape="RoundRectangle 15"
        StrokeThickness="1"
        Margin="10"
        Padding="20"
        VerticalOptions="Fill"
        HorizontalOptions="Fill">

            <VerticalStackLayout Spacing="10">

                <!-- Headers -->
                <FlexLayout Direction="Row"
                    JustifyContent="SpaceAround"
                    AlignItems="Start"
                    Wrap="NoWrap"
                    >
                    <Label Text="DATE" FontSize="30" FontAttributes="Bold"/>
                    <Label Text="POSITION" FontSize="30" FontAttributes="Bold"/>
                    <Label Text="START" FontSize="30" FontAttributes="Bold"/>
                    <Label Text="END" FontSize="30" FontAttributes="Bold"/>
                </FlexLayout>

                <!-- Rows of schedule -->
                <StackLayout BindableLayout.ItemsSource="{Binding SelectedPerson.shifts}"
                     Spacing="5">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <FlexLayout Direction="Row"
                                JustifyContent="SpaceAround"
                                AlignItems="Start"
                                Wrap="NoWrap"
                                >
                                <Label Text="{Binding Date}" FontSize="20" WidthRequest="90" HorizontalTextAlignment="Center"/>
                                <Label Text="{Binding Position}" FontSize="20" WidthRequest="100" HorizontalTextAlignment="Center"/>
                                <Label Text="{Binding start}" FontSize="20"  HorizontalTextAlignment="End" WidthRequest="90"/>
                                <Label Text="{Binding end}" FontSize="20"  HorizontalTextAlignment="End" WidthRequest="90"/>
                            </FlexLayout>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </StackLayout>

            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>
