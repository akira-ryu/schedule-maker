<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:subcinctus_factorem"
             x:Class="subcinctus_factorem.MainPage">

    <Grid RowDefinitions="Auto,*">

        <!-- Horizontal list of person buttons -->
        <CollectionView Grid.Row="0"
                ItemsSource="{Binding employees}"
                SelectionMode="Single"
                SelectedItem="{Binding SelectedPerson}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Horizontal"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Button Text="{Binding Title}" 
            Margin="10"
            Command="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=SelectPersonCommand}"
            CommandParameter="{Binding}"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Schedule grid for the selected person -->
        <Border Grid.Row="1"
        Stroke="LightGray"
        StrokeShape="RoundRectangle 15"
        StrokeThickness="1"
        Margin="10"
        Padding="20"
        VerticalOptions="Fill"
        HorizontalOptions="Fill">

            <VerticalStackLayout Spacing="10">

                <!-- Headers -->
                <FlexLayout Direction="Row"
                    JustifyContent="SpaceAround"
                    AlignItems="Start"
                    Wrap="NoWrap"
                    Margin="0,0,0,10"
                    >
                    <Label Text="Date" FontSize="30" FontAttributes="Bold"/>
                    <Label Text="Position" FontSize="30" FontAttributes="Bold"/>
                    <Label Text="Start" FontSize="30" FontAttributes="Bold"/>
                    <Label Text="End" FontSize="30" FontAttributes="Bold"/>
                </FlexLayout>

                <!-- Rows of schedule -->
                <StackLayout BindableLayout.ItemsSource="{Binding CurrentWeek}"
                     Spacing="5">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <FlexLayout Direction="Row"
                                JustifyContent="SpaceAround"
                                AlignItems="Start"
                                Wrap="NoWrap">
                                <Label Text="{Binding ., StringFormat='{0:MMM dd}'}" FontSize="20" WidthRequest="120" HorizontalTextAlignment="Center"/>
                                <Picker FontSize="14"  WidthRequest="190" HorizontalTextAlignment="Center"
                                  ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=MyOptions}"
                                  SelectedItem="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=SelectedOption}" />
                                <Picker FontSize="14"  WidthRequest="120" HorizontalTextAlignment="Center"
                                  ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=MyOptions}"
                                  SelectedItem="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=SelectedOption}" />
                                <Picker FontSize="14"  WidthRequest="120" HorizontalTextAlignment="Center"
                                  ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=MyOptions}"
                                  SelectedItem="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=SelectedOption}" />
                            </FlexLayout>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </StackLayout>

            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>
